---
# Inventory file defining proxy servers and test websites
all:
  children:
    proxy_servers:
      hosts:
        proxy1:
          ansible_host: localhost
          proxy_url: "http://proxy1.example.com:8080"
          proxy_username: "user1"
          proxy_password: "pass1"
          enabled: true
        proxy2:
          ansible_host: localhost
          proxy_url: "http://proxy2.example.com:8080"
          proxy_username: "user2"
          proxy_password: "pass2"
          enabled: true
        proxy3:
          ansible_host: localhost
          proxy_url: "http://proxy3.example.com:3128"
          proxy_username: ""
          proxy_password: ""
          enabled: false
    
    test_websites:
      hosts:
        google:
          test_url: "https://www.google.com"
          expected_status: 200
          request_timeout: 10
        github:
          test_url: "https://github.com"
          expected_status: 200
          request_timeout: 15
        baidu:
          test_url: "https://www.baidu.com"
          expected_status: 200
          request_timeout: 10
        httpbin:
          test_url: "https://httpbin.org/status/200"
          expected_status: 200
          request_timeout: 5